kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /home/mysql/data
        containerPath: /data/mysql
    
# kind create cluster --config kind-config.yaml