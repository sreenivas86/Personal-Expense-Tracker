apiVersion: v1
kind: Service
metadata:
  name: mysql-sfs
  namespace: e-track-dev
spec:
  clusterIP: None
  selector:
    app: mysql-sfs
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql