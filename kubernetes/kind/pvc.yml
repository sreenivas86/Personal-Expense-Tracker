apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: e-track
spec:
  accessModes:
  - ReadWriteOnce

  storageClassName: local-storage

  resources:
    requests:
      storage: 1Gi