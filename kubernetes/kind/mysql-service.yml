apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: e-track
spec:
  selector:
    app: mysql
  clusterIP: None   # headless service - correct for StatefulSet
  ports:
    - port: 3306
      targetPort: 3306