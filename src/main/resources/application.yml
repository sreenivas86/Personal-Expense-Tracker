spring:
  application:
    name:  ${APP_NAME:Expense-Tracker567}

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/expense_tracker}
    username: ${DB_USERNAME:chandu}
    password: ${DB_PASSWORD:1234}

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${APP_PORT:8080}