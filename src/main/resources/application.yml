spring:
  application:
    name:  ${APP_NAME:Expense-Tracker567}

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:expense_tracker}
    username: ${DB_USERNAME:chandu}
    password: ${DB_PASSWORD:1234}

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${APP_PORT:8080}

frontend:
  cors: 
    list: http://localhost:5173,http://localhost:3000,http://localhost:5000
